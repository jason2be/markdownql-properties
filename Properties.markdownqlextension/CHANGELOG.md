# Changelog

## 1.1.1

- Publish the independently reviewed draft lifecycle, explicit-save, conflict-recovery, validation, and host-owned mutation boundary as the standalone Properties extension release.

## 1.1.0

- Keep text, list, number, date, and tag edits as generation-scoped local drafts until an explicit save action.
- Adopt the host-normalized snapshot after successful revision-bound saves, and preserve drafts across stale-revision conflicts without automatic retries.
- Confirm destructive removal, surface recoverable collection errors, and discard late results from superseded window or document generations.

## 1.0.5

- Localize the workspace collection through the versioned workspace-surface context.

## 1.0.4

- Localize the document, Home, and collection surface titles through the app language table.

## 1.0.3

- Keep the collection filter and sort placeholder choices within the Extension surface protocol so the 4.8 collection opens reliably.

## 1.0.2

- Restore the typed document editor, compact workspace entry, and the complete 4.8 collection filtering, sorting, and navigation behavior.

## 1.0.1

- Add the first host-governed evolution baseline.
